# AfriMeet AI — Web Frontend

Placeholder. Planned stack: React / Next.js (see project root README, Phase 6).
Not yet implemented — the current focus is the ASR data pipeline and model
(`src/afrimeet/data`, `src/afrimeet/models`) and the API (`src/afrimeet/api`).
